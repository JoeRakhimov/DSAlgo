import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "function")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Let's learn data structures and algorithms!")
        }
    }
}
